func AddTwo (a) {
  a = a + a + (a),
  return a + 2,
}

AddOne (a) {
  return a + 1,
}

main () { 
  var a = exec AddOne(1),
  var b = exec AddTwo(1),
  if a > 5 { 
    print("True"),
  }
  else {
    print("False"),
}